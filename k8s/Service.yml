apiVersion: v1
kind: Service
metadata:
  name: nodejs-sample-service
  labels:
    app: nodejs-sample-service
spec:
  selector:
    app: nodejs-sample
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP